name: twenty_nine_card_game
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.18.0
  file_picker: ^10.3.3
  csv: ^6.0.0
  charts_flutter: ^0.12.0

  # Firebase packages
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  firebase_database: ^12.0.3
  firebase_auth: ^6.1.1

  # Icons
  cupertino_icons: ^1.0.8

  # Multiplayer dependencies (Phase 6)
  flutter_bluetooth_serial: ^0.4.0
  nearby_connections: ^4.3.0

  # Unique IDs for players
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_cloud_firestore: ^4.0.0
  firebase_auth_mocks: ^0.15.1
  test: ^1.24.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cards/